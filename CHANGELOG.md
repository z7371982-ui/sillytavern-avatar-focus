# 更新记录

## 1.2.12

- 去掉缩小头像时叠加的同图铺满底层，消除双层头像和边缘重影。
- 缩放保留单张图片、原圆角和遮罩；图片未覆盖的区域显示框内背景。

## 1.2.11

- 开关改为只控制长按、三击打开面板；关闭后已保存的取景、缩放和饱和度仍然生效。
- 刷新页面、新消息加载和头像尺寸变化时，仍正常应用保存的效果。

## 1.2.10

- 修复放大头像时使用方形裁剪导致圆框变方的问题：在原元素内部缩放画面，保留主题圆角、遮罩和外框。
- 调整预览使用同样的框内缩放方式；图片加载完成和头像尺寸变化后重新计算显示。
- 扩展设置提供明确的总开关，同时控制长按、三击和已保存的显示效果；关闭保留设置，重新开启恢复应用。

## 1.2.9

- 新增单张头像饱和度调整，范围为 0%–300%；0% 可转为黑白，100% 可恢复原图颜色，更高数值可增强显色。
- 新增“原色 100%”快捷按钮，调整过程实时预览，取消会恢复调整前状态。
- 饱和度按头像库中的具体图片分别保存，切换图片不会互相影响；设置只作用于页面显示，不修改原图文件。
- 兼容主题自带的 `saturate()` 与 `grayscale()` 滤镜，同时保留亮度、对比度和阴影等其它滤镜。

## 1.2.8

- 修复某些主题的头像装饰容器覆盖正文时，长按正文也会误打开头像调整的问题。
- 长按触发范围改为头像当前实际显示矩形及周围 8px；滚动或布局变化后会实时重新计算，不依赖固定页面坐标。
- 移除消息头像外层大容器的宽范围兜底，正文长按恢复浏览器原生选择与复制。
- 不再永久禁止头像外层容器中的文字选择；只在确认按中头像后临时屏蔽当前手势的选择行为。

## 1.2.7

- 修复：user 头像默认 100% 使用等比例 2:3 铺满图，一开始就填满头像框，不需要手动放大。
- 修复：头像库完整原图只在缩小到 100% 以下时用于取景，50% 时完整显示；空出区域由同图铺满层承接，不露白。
- 保留：头像 `src`、身份识别、长按计时、触摸事件和编辑弹窗仍沿用 1.2.0 实现。

## 1.2.6

- 修复：user 头像上传前生成标准 2:3 PNG，完整原图等比例居中，避免酒馆后端强制转换到 400×600 时压瘪变形。
- 修复：原图之外使用同图铺满层托底，不裁掉原图边缘，也不使用白色填充。
- 修复：长按缩小到 100% 以下时，在原头像元素背后增加不接收点击的同图底层，避免头像框四周露白。
- 保留：1.2.0 的长按识别、按压计时、手势事件和编辑弹窗逻辑不变。

## 1.2.5

- 回退：`index.js` 完整恢复为已确认可长按的 1.2.0 实际文件，撤销 1.2.2–1.2.4 对头像身份、`blob:` 原图映射、缩放渲染、DOM 观察和初始化的所有改动。
- 恢复：沿用 1.2.0 原长按、原三击头像库、原左右切换逻辑。

## 1.2.0

- 新增：三击头像打开角色或用户的专属头像库。
- 新增：支持一次多选导入头像图片，并使用左右箭头逐张翻看。
- 新增：点“使用这张”即可切换，之后无需重新从设备选择文件。
- 新增：空头像库首次打开时自动保存当前头像，方便随时切回。
- 存储：图片保存在当前浏览器 IndexedDB 中，不写入酒馆设置文件。
- 修复：相册框按图片比例收缩，移除多余的上下留白；手机端底部“使用这张”始终保留在可见区域。
- 修复：相册图片锁定原始宽高比，只做等比例缩放，避免被移动端主题样式压扁或拉长。

## 1.1.3

- 新增：头像支持 50%–300% 放大缩小，缩放比例按头像单独保存。
- 兼容：旧版只包含横纵位置的保存记录会自动按 100% 缩放继续使用。
- 修复：放大头像时将画面裁切在头像原有边界内，不再溢出到消息和主题装饰区域。
- 修复：美化头像视觉位置与原 DOM 点击层错位时，按触摸坐标反查真正头像。
- 修复：长按用户头像时不再选中头像下方的消息文本。
- 兼容：为不支持 Pointer Events 的旧 WebView 增加触摸事件后备。
- 修复：Termux 手机浏览器动态地址栏导致调整窗口底部显示不完整。
- 优化：移动端弹窗保持居中，标题和操作按钮保持可见，中间内容独立滚动。
- 兼容：同步手机浏览器 visualViewport 的宽、高和顶部偏移，避免 Termux 访问时弹窗被浏览器界面裁切。
- 优化：长按头像改为静态高亮，避免不规则美化头像框反复缩放抖动。
- 优化：移除头像外层容器的长按选择框，避免部分主题显示过大的矩形边框。
- 修复：模板文件从插件自身目录读取，设置面板加载失败后不再被 DOM 观察器无限重试，避免错误提示反复叠加。

## 1.1.2

- 修复：长按命中美化头像的外框、遮罩或装饰层时，也会自动找到其中真正的头像图片。
- 兼容：扩展常见自定义头像容器，不再局限于酒馆原生的 .avatar img。
- 优化：触屏长按允许更自然的轻微手指移动，并在命中外框时显示反馈描边。

## 1.1.1

- 修复：移除仅部分 TauriTavern 版本提供的前端导出，避免标准 SillyTavern 因模块导入失败而完全不加载。
- 调整：角色头像替换后的缓存刷新由扩展自身完成。

## 1.1.0

- 新增：连续三击角色头像，直接选择图片并替换角色头像。
- 新增：连续三击用户头像，调用酒馆自带上传/裁剪流程。
- 新增：可在扩展设置中单独关闭三击替换功能。
- 保留：长按头像后拖动或使用滑杆调整取景。

## 1.0.0

- 首次发布。
- 支持按头像分别保存 object-position。
- 支持聊天区、角色列表和用户头像列表。
